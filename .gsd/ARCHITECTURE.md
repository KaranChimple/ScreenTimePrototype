# Architecture

> Auto-generated by /map on 2026-02-24

## Overview

ScreenTimePrototype is a **fresh React Native 0.84 scaffold** with a Swift-based iOS layer. It currently contains only boilerplate code — no custom components, no native modules, and no business logic.

```
┌─────────────────────────────────────────┐
│     index.js (RN Entry Point)           │
├─────────────────────────────────────────┤
│     App.tsx (Root Component)            │
│     ├── SafeAreaProvider                │
│     └── NewAppScreen (boilerplate)      │
├─────────────────────────────────────────┤
│     iOS Native Layer                    │
│     └── AppDelegate.swift               │
│         (RCTReactNativeFactory)         │
└─────────────────────────────────────────┘
```

## Components

### React Native Layer

#### Entry Point — `index.js`
- **Purpose:** Registers `App` component with AppRegistry
- **Module name:** `ScreenTimePrototype`

#### Root Component — `App.tsx`
- **Purpose:** Default boilerplate screen
- **Dependencies:** `react-native-safe-area-context`, `@react-native/new-app-screen`
- **Features:** Dark mode detection via `useColorScheme()`

### iOS Native Layer

#### AppDelegate — `ios/ScreenTimePrototype/AppDelegate.swift`
- **Purpose:** Standard RN app delegate using `RCTReactNativeFactory` (new architecture)
- **Pattern:** Uses `ReactNativeDelegate` with `RCTDefaultReactNativeFactoryDelegate`
- **Bundle:** `ScreenTimePrototype` module name

### Tests

#### `__tests__/App.test.tsx`
- **Purpose:** Single smoke test — renders `App` and verifies no crash
- **Framework:** Jest + react-test-renderer

## Data Flow

Currently trivial:
1. `index.js` → registers `App` component
2. `App.tsx` → renders `SafeAreaProvider` → `NewAppScreen` (boilerplate)
3. No native bridge calls exist yet

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| None | — | No external integrations |

## Technical Debt

- None (fresh scaffold)

## Conventions

**Language:** TypeScript (React Native), Swift (iOS native)
**Architecture:** React Native 0.84 New Architecture (`RCTReactNativeFactory`)
**Module System:** CommonJS (`index.js`), ES Modules (`App.tsx`)
**Testing:** Jest with react-test-renderer
**Styling:** React Native `StyleSheet.create()`
